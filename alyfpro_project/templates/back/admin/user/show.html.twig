{% extends 'back/base.html.twig' %}

{% block body %}
    <div class="d-flex justify-content-center pb-3 mt-5">
    <h1 class="text-dark-emphasis mx-2">{{ user.lastName }} {{ user.firstName }}</h1> <div style="width: 70px; height: 70px; overflow: hidden; border-radius: 50%; border: 1px solid black">
    {% if user.image is not empty %}
        <img src="{{ asset(user.image) }}" alt="User Image" style="width: 100%; height: 100%; object-fit: cover;">
    {% else %}
        <img src="{{ asset('images/icons/logo2.png') }}" alt="User Image" style="width: 100%; height: 100%; object-fit: cover;">
        <div style="width: 100%; height: 100%; background-color: #ccc;"></div>
    {% endif %}
</div>
        <a href="{{ path('app_admin_user_edit', {'id': user.id}) }}" class="btn btn-warning "><i class="fas fa-pen  "></i>Edit</a>
</div>
    <table class="table table-dark table-striped">
        <thead>
        <tr>
            <th>Id</th>
            <th>Nom</th>
            <th>Email</th>
            <th>Password</th>
            <th>Image</th>
            <th>Roles</th>
            <th>Specialities</th>


        </tr>
        </thead>

        <tbody>
            <tr>

                <td>{{ user.id }}</td>
                <td>{{ user.lastName }} {{ user.firstName }}</td>

                <td>{{ user.email }}</td>

                <td>{{ user.password }}</td>
                <td>{{ user.image }}</td>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
                <td>
                    {% for specialty in user.specialities %}
                        {{ specialty.title }}
                        {% if not loop.last %}, {% endif %}
                    {% endfor %}
                </td>
            </tr>

        </tbody>
    </table>
<div class="d-flex justify-content-evenly">
    <a href="{{ path('app_admin_user_index') }}" class="btn btn-outline-dark"><i class="fa fa-arrow-left "></i>back to list</a>

    {{ include('back/admin/user/_delete_form.html.twig') }}
</div>

{# ***************************Form card, si le admin prefere afficher les infos de chaque formateur
*************************de la forme d'une carte au lieu d'une liste ************************************ #}
{#    <div class="card center mx-3 mt-2" style="width: 50rem; height: auto;">#}
{#        <div class="card-body">#}
{#            <div class="text-center">#}
{#                {% if user.image is not empty %}#}
{#                    <img src="{{ asset(user.image) }}" alt="User Image" class="rounded-circle border" style="width: 100px; height: 100px; object-fit: cover;">#}
{#                {% else %}#}
{#                    <img src="{{ asset('images/icons/logo2.png') }}" alt="User Image" class="rounded-circle border" style="width: 100px; height: 100px; object-fit: cover;">#}
{#                {% endif %}#}
{#            </div>#}

{#            <h1 class="text-dark-emphasis text-center my-3">{{ user.lastName }} {{ user.firstName }}</h1>#}

{#            <div class="user-details text-center">#}
{#                <p class="card-text">#}
{#                    <strong>ID:</strong> {{ user.id }}<br>#}
{#                    <strong>Email:</strong> {{ user.email }}<br>#}
{#                    <strong>Mot de passe:</strong> {{ user.password }}<br>#}
{#                    <strong>URL Image:</strong> {{ user.image }}<br>#}
{#                    <strong>Roles:</strong> {{ user.roles ? user.roles|json_encode : '' }}<br>#}
{#                    <strong>Spécialités:</strong>#}

{#                    {% for specialty in user.specialities %}#}
{#                        {{ specialty.title }}#}
{#                        {% if not loop.last %}, {% endif %}#}
{#                    {% endfor %}#}
{#                </p>#}
{#            </div>#}

{#            <div class="d-flex justify-content-evenly mt-3">#}
{#                <a href="{{ path('app_admin_user_index') }}" class="btn btn-outline-dark">#}
{#                    <i class="fa fa-arrow-left"></i> Retour à la liste#}
{#                </a>#}

{#                {{ include('back/admin/user/_delete_form.html.twig') }}#}
{#            </div>#}
{#        </div>#}
{#    </div>#}

{% endblock %}
