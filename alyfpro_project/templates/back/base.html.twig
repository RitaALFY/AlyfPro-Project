<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <link rel="shortcut icon" type="image/png" href="{{ asset('images/icons/logo2.png') }}">
    {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
    {% block title %}
        <div class="admin-head pt-2 pb-2">
            <h2 class="admin-title">Administration</h2>
            <div class="d-flex justify-content-around align-items-center">
                <a class="nav-item nav-link text-white" href="{{ path('app_planning') }}">
                    <i class="fas fa-list-squares"></i> Planning
                </a>
                <a class="nav-item nav-link text-white" href="{{ path('app_logout') }}">
                    <i class="fas fa-sign-out-alt"></i> Déconnexion
                </a>

            </div>
        </div>

    {% endblock %}

    {% block stylesheets %}
        {{ encore_entry_link_tags('back_styles') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('back_scripts') }}
    {% endblock %}
</head>
<body style="background-color: #FFFFFF !important;">

<div class="container-fluid">
    <div class="row">
        <div class="col-lg-3 col-md-3 col-sm-12 admin-menu">
            {% block menu %}
                {% include 'back/admin/common/menu.html.twig' %}
            {% endblock %}
        </div>
        <div class="col-lg-9 col-md-9 col-sm-12 pe-4">
            <main class="mt-3">
                {% block body %}
                {% endblock %}
            </main>
        </div>
    </div>
</div>
</body>
</html>
