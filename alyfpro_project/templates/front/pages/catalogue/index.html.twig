{% extends 'front/base.html.twig' %}
{% block body %}
    <section id="catalogue">
        <div>
            <h4 class="pt-2 pb-3">Catalogue</h4>
        </div>
<div class="container">
            <div class="row">
                {% for category in categories %}
                    <div class="col-md-3 mb-5"> {# Utilisez la classe "col-md-3" pour avoir 4 cartes par ligne sur un écran de taille moyenne (md) #}
                        <div class="card">
                            <img src=" {{ category.image }}" alt="{{ category.title }}" class="card-img-top" height="200px" width="100px">
{#                            <div class="card-body">#}
{#                                <h5 class="card-title">{{ category.title }}</h5>#}
                                <a href="{{ path('app_catalogue_show', {'id': category.id}) }}" class="btn btn-blue">{{ category.title }}</a>
{#                            </div>#}
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </section>
{% endblock %}